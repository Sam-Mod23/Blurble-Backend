{
  "name": "mongoose-test-scenarios",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "db-start-linux": "mongod",
    "db-start-mac": "brew services start mongodb-community@4.4",
    "db-stop-mac": "brew services stop mongodb-community@4.4",
    "drop-db": "node ./utils/drop-db.js",
    "seed": "node app.js",
    "start": "npm run db-start-linux && npm run drop-db && npm run seed",
    "start-mac": "npm run db-start-mac && npm run drop-db && npm run seed"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mongodb": "^3.6.6",
    "mongoose": "^5.12.4",
    "mongoose-auto-increment": "^5.0.1",
    "node": "^15.14.0"
  }
}
